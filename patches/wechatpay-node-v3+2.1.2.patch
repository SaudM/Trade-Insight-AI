diff --git a/node_modules/wechatpay-node-v3/src/index.js b/node_modules/wechatpay-node-v3/src/index.js
index 10a778c..b5b63cf 100644
--- a/node_modules/wechatpay-node-v3/src/index.js
+++ b/node_modules/wechatpay-node-v3/src/index.js
@@ -2,7 +2,7 @@
 
 const Utils = require('./utils');
 const Aes = require('./aes');
-const Formidable = require('./formidable');
+// const Formidable = require('./formidable');
 
 /**
  * 返参处理器
@@ -102,7 +102,7 @@
    * @property {string} v3key      - APIv3密钥，用于解密回调通知
    * @property {string} [signType] - 签名类型，支持HMAC-SHA256和RSA-SHA256，默认RSA-SHA256
    */
-  constructor(config = {}, formidable = {}) {
+  constructor(config = {}) {
     // 可选参数
     const {
       signType = 'RSA-SHA256',
@@ -137,7 +137,7 @@
     this.aes = new Aes(this.v3key);
 
     // 回调通知解析器
-    this.notification = new Formidable(formidable, this).parse();
+    // this.notification = new Formidable(formidable, this).parse();
 
     // `v2`支付模块
     this.v2 = {
diff --git a/node_modules/wechatpay-node-v3/src/index.mjs b/node_modules/wechatpay-node-v3/src/index.mjs
index 725d2c2..47053e1 100644
--- a/node_modules/wechatpay-node-v3/src/index.mjs
+++ b/node_modules/wechatpay-node-v3/src/index.mjs
@@ -1,6 +1,6 @@
 import Utils from './utils.mjs';
 import Aes from './aes.mjs';
-import Formidable from './formidable.mjs';
+// import Formidable from './formidable.mjs';
 
 /**
  * 返参处理器
@@ -101,7 +101,7 @@
    * @property {string} v3key      - APIv3密钥，用于解密回调通知
    * @property {string} [signType] - 签名类型，支持HMAC-SHA256和RSA-SHA256，默认RSA-SHA256
    */
-  constructor(config = {}, formidable = {}) {
+  constructor(config = {}) {
     // 可选参数
     const {
       signType = 'RSA-SHA256',
@@ -136,7 +136,7 @@
     this.aes = new Aes(this.v3key);
 
     // 回调通知解析器
-    this.notification = new Formidable(formidable, this).parse();
+    // this.notification = new Formidable(formidable, this).parse();
 
     // `v2`支付模块
     this.v2 = {
